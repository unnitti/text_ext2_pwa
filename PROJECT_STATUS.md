# Project Status

## 현재 버전
- 앱 버전: `v0.1`
- 서비스 워커 캐시: `clipboard-processor-v5`

## 최근 변경 사항
기존 웹앱 내장 OCR 방식 대신, iPhone 단축어의 라이브 텍스트를 인코딩하여 URL로 전달하는 방식으로 변경함

- 단축어에서 추출한 라이브 텍스트를 URL로 전달
- URL 끝에 `/#t=`를 추가하여 웹앱에서 전달된 텍스트를 클립보드 내용으로 자동 처리

## 정상 작동하는 기능
클립보드의 텍스트에서 정해진 규칙에 맞는 정보를 추출하고 원하는 형식으로 재가공하여 클립보드에 복사함

- `상품코드` 뒤의 영문 대문자와 숫자로 구성된 12자리 코드 추출
  - 중간에 포함된 공백 제거 후 하나의 코드로 결합
- `소비기한` 또는 `제조일자` 뒤의 `YYYY-MM-DD` 형식 날짜 추출
  - `소비 26.09.12` 또는 `제조 26.09.12` 형식으로 변환
- `지번` 뒤의 텍스트 추출
  - 앞의 3글자 제외
- 결과를 **상품코드 → 날짜 → 지번** 순서로 재정렬
- 가공한 결과를 클립보드에 복사

## 미확인 기능
- 현재 없음 (iPhone 기준)
- Android 기기 테스트는 아직 하지 않음

## 알려진 버그
- 현재까지 발견된 버그 없음
- 실제 사용 빈도가 제한적이어서 특정 상황의 미발견 버그가 있을 수 있음

## 실제 기기 테스트 결과
- iPhone: 정상 작동 확인
- Android: 미확인

## 다음 작업
- 특수문자 쌍이 맞지 않는 문제를 해결하기 위해 BASE64 인코딩 방식으로 변경 검토
- 브라우저 글씨 크기 때문에 12자리 상품코드가 두 줄로 표시되는 경우 하나의 코드로 결합
- `보충` 이후의 내용이 결과에 포함되지 않도록 조건문 수정

## 변경 시 주의
- 현재 정상 작동하는 기능은 요청 없이 변경하지 않는다.
- 코드 파일의 줄바꿈은 **LF로 통일**한다.
- CRLF와 LF를 혼용하지 않는다.
