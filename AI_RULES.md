# AI 협업 주의사항

이 프로젝트는 ChatGPT, Claude, Gemini 등 여러 AI가 번갈아 작업할 수 있다.

## 1. 현재 상태를 먼저 확인한다

작업 전 반드시 **최신 GitHub 코드와 `PROJECT_STATUS.md`**를 확인한다.

- 과거 대화나 AI의 기억보다 현재 코드를 우선한다.
- 이전 AI가 어떻게 수정했는지 추측하지 않는다.
- 실제 파일과 현재 프로젝트 상태를 기준으로 판단한다.

## 2. 요청한 부분만 수정한다

- 기존 기능을 유지한다.
- 요청하지 않은 리팩터링이나 구조 변경을 하지 않는다.
- 관련 없는 코드를 임의로 정리하거나 개선하지 않는다.
- 기존 동작에 영향을 줄 가능성이 있으면 먼저 알린다.

## 3. 코드 분석과 실제 테스트를 구분한다

AI가 직접 실제 기기에서 테스트하지 않았다면 **실제 작동을 보장하지 않는다.**

특히 PWA는 다음 환경에서 문제가 발생할 수 있다.

`코드 → GitHub Pages → 서비스 워커 → 캐시 → iPhone Safari → 실제 사용`

따라서 다음을 구분한다.

- "코드상으로는 문제가 없어 보인다."
- "실제 iPhone에서 정상 작동한다."

사용자가 실제 기기에서 확인한 결과를 중요한 사실로 취급하고, 문제가 있으면 그 결과를 기준으로 다시 분석한다.

## 4. 앱 버전과 캐시 버전을 구분한다

앱 기능 버전과 서비스 워커 캐시 버전은 별도로 관리한다.

- 기능 변경이 있을 때만 앱 버전을 변경한다.
- 캐시 갱신이 필요할 때만 캐시 버전을 변경한다.
- 테스트나 작은 수정 때문에 버전을 의미 없이 올리지 않는다.

## 5. Git을 공통 변경 이력으로 사용한다

가능하면 다음 순서로 작업한다.

`현재 상태 확인 → 수정 → 테스트 → 결과 확인 → commit → 다음 AI에게 전달`

문제가 생기면 Git commit을 기준으로 변경 사항을 추적한다.

## 6. AI끼리 동시에 수정하지 않는다

같은 프로젝트를 여러 AI가 동시에 수정하지 않는다.

가능하면 한 AI의 작업과 commit이 끝난 후 다음 AI에게 넘긴다.

`ChatGPT → commit → Claude → commit → Gemini → commit`

필요하면 한 AI는 수정하지 않고 **코드 검토만** 담당할 수 있다.

## 7. `PROJECT_STATUS.md`를 인수인계 문서로 사용한다

문서는 짧게 유지하고 다음 정보만 기록한다.

- 현재 앱 버전 / 캐시 버전
- 최근 변경 사항
- 정상 작동하는 기능
- 미확인 기능
- 알려진 버그
- 실제 기기 테스트 결과
- 다음 작업
- 변경하면 안 되는 부분

## 8. 코드 파일의 줄바꿈은 LF로 통일한다. CRLF와 LF를 혼용하지 않는다.

**핵심 원칙: 최신 GitHub 코드 + `PROJECT_STATUS.md`가 기준이다. 과거 AI의 기억이나 추측은 기준으로 사용하지 않는다.**
